class EmailSenderService {
  // on envoi un email quand la todoList atteint 8 items
  sendAlmostFull(user, todoList) {
    throw new Error("sendAlmostFull n'est pas implémenté (doit être mocké en test).");
  }
}

module.exports = EmailSenderService;